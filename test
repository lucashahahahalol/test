---@diagnostic disable: undefined-global


loadstring(game:HttpGet("https://pastebin.com/raw/15bT0XYm"))()

-- ══════════════════════════════════════════
--         CONFIG SYSTEM SETUP
-- ══════════════════════════════════════════
local CONFIG_FOLDER = "ZyrcHub"
local DEFAULT_CONFIG = "config"

if not isfolder(CONFIG_FOLDER) then
	makefolder(CONFIG_FOLDER)
end

-- ══════════════════════════════════════════
--         SERVICES & CORE VARS
-- ══════════════════════════════════════════
local plrservice = game:GetService("Players") 
local plr = plrservice:GetChildren()
local HumanoidRootPart
pcall(function()
	HumanoidRootPart = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
end)

task.spawn(function()
	while true do
		pcall(function()
			HumanoidRootPart = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
		end)
		task.wait()
	end
end)
local Huge = math.Huge


local VIM = game:GetService("VirtualInputManager")

local function SkillBind(bind)
	pcall(function()
		VIM:SendKeyEvent(true, bind, false, game)
		task.wait()
		VIM:SendKeyEvent(false, bind, false, game)
		wait(.2)
	end)
end

local function MainMenuPress()
	SkillBind(Enum.KeyCode.BackSlash)
	task.wait(0.2)
	SkillBind(Enum.KeyCode.Return)
	task.wait(0.2)
	SkillBind(Enum.KeyCode.BackSlash)
end

if game.Players.LocalPlayer.PlayerGui:FindFirstChild("Main Menu") then
	wait(0.3)
	MainMenuPress()
end

repeat wait() until game:IsLoaded() 

local MAX_DISTANCE = math.huge 

local function HoldE()
	VIM:SendKeyEvent(true, Enum.KeyCode.E, false, game)
	task.wait(0.7)
	VIM:SendKeyEvent(false, Enum.KeyCode.E, false, game)
end

local function getClosestMob()
	local character = game.Players.LocalPlayer.Character
	if not character then return nil end

	local myRoot = character:FindFirstChild("HumanoidRootPart")
	if not myRoot then return nil end

	local liveFolder = workspace:FindFirstChild("Live")
	if not liveFolder then return nil end

	local closestMob = nil
	local closestDistance = math.huge

	for _, model in ipairs(liveFolder:GetDescendants()) do
		if not model:IsA("Model") then
			continue
		end

		-- Skip player characters
		if game.Players:GetPlayerFromCharacter(model) then
			continue
		end

		local humanoid = model:FindFirstChildOfClass("Humanoid")
		local root = model:FindFirstChild("HumanoidRootPart")

		if humanoid and root and humanoid.Health > 0 then
			local distance = (myRoot.Position - root.Position).Magnitude

			if distance < closestDistance and distance <= MAX_DISTANCE then
				closestDistance = distance
				closestMob = model
			end
		end
	end

	return closestMob
end

-- ══════════════════════════════════════════
--         UI SETUP
-- ══════════════════════════════════════════
local lib = loadstring(game:HttpGet("https://raw.githubusercontent.com/RandomRandom123-jacks/Scripts/refs/heads/main/PEPSIUILIBLUA.lua"))()
local subs = lib.subs 
local libclose = subs.Wait

local window = lib:CreateWindow({
	Name = "Zyrc Hub",
	Themeable = {
		Info = {"KONTOL"},
		Credit = false, 
		Background = "",
		Visible = true
	}
})

local main1 = window:CreateTab({ Name = "Auto Farm" })
local main3 = window:CreateTab({ Name = "Miscellaneous"})
local main2 = window:CreateTab({ Name = "Config" })


local section1  = main1:CreateSection({ Name = "Auto Farm" })
local section2  = main1:CreateSection({ Name = "Auto's", Side = "Right" })
local section4  = main1:CreateSection({ Name = "Raids", Side = "Right" })
local section9  = main1:CreateSection({ Name = "Shop Snipe" })
local section5  = main1:CreateSection({ Name = "TPS" })
local section6  = main1:CreateSection({ Name = "Auto EXP" })
local section7  = main3:CreateSection({ Name = "Auto Sell", Side = "Right" })
local section8  = main3:CreateSection({ Name = "Auto Open Chests" })
local section10 = main3:CreateSection({ Name = "Misc" })
local section11  = main3:CreateSection({ Name = "Auto Roll", Side = "Right" })
local section12  = main3:CreateSection({ Name = "Auto Workouts" })
-- ══════════════════════════════════════════
--         CONFIG SECTION UI
-- ══════════════════════════════════════════
local configSection = main2:CreateSection({ Name = "Config" })


-- ══════════════════════════════════════════
--         SHOP BUTTONS
-- ══════════════════════════════════════════
section12:AddButton({
	Name = "Treadmill",
	Callback = function()
		HumanoidRootPart.CFrame = CFrame.new(workspace:WaitForChild("Map"):FindFirstChild("Workouts"):FindFirstChild("Treadmill"):FindFirstChild("Main").Position)
		wait(0.1)
		HoldE()
	end
})

section12:AddButton({
	Name = "Situps",
	Callback = function()
		HumanoidRootPart.CFrame = CFrame.new(workspace:WaitForChild("Map"):FindFirstChild("Workouts"):FindFirstChild("Situps"):FindFirstChild("Main").Position)
		wait(0.1)
		HoldE()
	end
})

section12:AddButton({
	Name = "Squats",
	Callback = function()
		HumanoidRootPart.CFrame = CFrame.new(workspace:WaitForChild("Map"):FindFirstChild("Workouts"):FindFirstChild("Squat Rack"):FindFirstChild("Main").Position)
		wait(0.1)
		HoldE()
	end
})

section12:AddButton({
	Name = "Dumb Bells",
	Callback = function()
		HumanoidRootPart.CFrame = CFrame.new(workspace:WaitForChild("Map"):FindFirstChild("Workouts"):FindFirstChild("Dumbbells"):FindFirstChild("Main").Position)
		wait(0.1)
		HoldE()
	end
})

section12:AddButton({
	Name = "Bench Press",
	Callback = function()
		HumanoidRootPart.CFrame = CFrame.new(workspace:WaitForChild("Map"):FindFirstChild("Workouts"):FindFirstChild("Bench Press"):FindFirstChild("Main").Position)
		wait(0.1)
		HoldE()
	end
})

section4:AddButton({
	Name = "Open Raid Shops",
	Callback = function()
		game.Players.LocalPlayer.PlayerGui:WaitForChild("Raid Shop"):WaitForChild("raid_shop").Visible = true
	end
})

section4:AddButton({
	Name = "Open Prestige Shops",
	Callback = function()
		game.Players.LocalPlayer.PlayerGui:WaitForChild("Prestige Shop"):WaitForChild("prestige_shop").Visible = true
	end
})

section4:AddButton({
	Name = "Open Cash Shops",
	Callback = function()
		game.Players.LocalPlayer.PlayerGui:WaitForChild("Cash Shop"):WaitForChild("cash_shop").Visible = true
	end
})

section4:AddButton({
	Name = "Open Crafting",
	Callback = function()
		game.Players.LocalPlayer.PlayerGui:WaitForChild("Crafting"):WaitForChild("holder").Visible = true
	end
})

section4:AddButton({
	Name = "Open Gang Shops",
	Callback = function()
		game.Players.LocalPlayer.PlayerGui:WaitForChild("Gang Shop"):WaitForChild("gang_shop").Visible = true
	end
})

section4:AddLabel("-------------------------")

-- ══════════════════════════════════════════
--         DATA / LISTS
-- ══════════════════════════════════════════
local xx = {}
local ff = {
	"Dr. Bosconovitch", 
	"Akira Otoishi",
	"Zombie Rudol von Stroheim",
	"Okuyasu Nijimura PRIME",
	"Miyamoto Musashi",
	"Yoshikage Kira"
}

local mm = {
	["Spin User"]         = Vector3.new(1823.47522, 979.471069, 1554.76904),
	["Cultist"]           = Vector3.new(1823.47522, 979.471069, 1554.76904),
	["Vampire"]           = Vector3.new(1823.47522, 979.471069, 1554.76904),
	["Elite Vampire"]     = Vector3.new(2125.83057, 910.52887, 1422.74438),
	["Zombie"]            = Vector3.new(2125.83057, 910.52887, 1422.74438),
	["Zombie Grunt"]      = Vector3.new(2125.83057, 910.52887, 1422.74438),
	["Rogue Rock Human"]  = Vector3.new(1633.09668, 933.146606, 1329.90857),
	["Hamon Apprentice"]  = Vector3.new(1633.09668, 933.146606, 1329.90857),
	["Night Vampire"]     = Vector3.new(1561.27332, 876.18512, 251.074799),
	["Thug"]              = Vector3.new(1561.27332, 876.18512, 251.074799),
	["Mafia Member"]      = Vector3.new(1561.27332, 876.18512, 251.074799),
	["Thief"]             = Vector3.new(1692.29956, 874.511658, 153.032227),
	["Samurai"]           = Vector3.new(1692.29956, 874.511658, 153.032227),
	["Samurai Master"]    = Vector3.new(1953.82605, 874.659729, -186.887161),
	["Delinquent"]        = Vector3.new(1383.68359, 881.792542, -485.135925),
	["Yakuza"]            = Vector3.new(1383.68359, 881.792542, -485.135925),
	["Corrupt Police"]    = Vector3.new(918.58252, 885.79248, -618.79834),
	["Prison Escapee"]    = Vector3.new(918.58252, 885.79248, -618.79834),
	["Elite Mafia Member"]= Vector3.new(918.58252, 885.79248, -618.79834),
	["Cyborg"]            = Vector3.new(618.588684, 886.061096, -184.376724),
}

local two = {
	"Spin User","Cultist","Vampire","Elite Vampire","Zombie","Zombie Grunt",
	"Rogue Rock Human","Hamon Apprentice","Night Vampire","Thug","Mafia Member",
	"Thief","Samurai","Samurai Master","Delinquent","Yakuza",
	"Corrupt Police","Prison Escapee","Cyborg","Elite Mafia Member",
}

local function addPlayer(name)
	table.insert(xx, name)
	table.sort(xx, function(a, b) return a:lower() < b:lower() end)
end

for i,v in pairs(game:GetService("ReplicatedStorage").assets["npc_cache"]:GetChildren()) do
	addPlayer(v.Name)
end

local AccesoryData = game:GetService("ReplicatedStorage").requests.miscellaneous:WaitForChild("get_data"):InvokeServer("accessory")
local ItemData = game:GetService("ReplicatedStorage").requests.miscellaneous:WaitForChild("get_data"):InvokeServer("item")
local Data = {}
task.spawn(function()
	for i,v in pairs(AccesoryData) do
		Data[tostring(i)] = {}
		for x,f in pairs(v) do
			Data[tostring(i)][x] = f
		end
	end
end)

local ItemList = {}

for itemName, _ in pairs(ItemData) do
	table.insert(ItemList, itemName)
end

-- ══════════════════════════════════════════
--         STATE VARS
-- ══════════════════════════════════════════
local savedtick = tick()
local curtarget = nil 
local Bossname
local disablewhenfarm = false
local farming = false
local Mobname
local Method
local Something
local Item
local Dist = 9
local Selectedtime = 0.1
local Selectedtime2 = 0.1
local bosstarget
local mobtarget
local l, k = false, false

-- ══════════════════════════════════════════
--         SECTION 1 — AUTO FARM
-- ══════════════════════════════════════════
section1:AddDropdown({
	Name = "Bosses", Nothing = "Select Boss..", List = ff, Flag = "Bosses", 
	Callback = function(selected)
		Bossname = selected
	end
})

section1:AddDropdown({
	Name = "NPCS", Nothing = "Select NPC..", List = two, Flag = "NPCS", 
	Callback = function(selected)
		Mobname = selected
		HumanoidRootPart.CFrame = CFrame.new(mm[tostring(Mobname)])
	end
})

section1:AddDropdown({
	Name = "Method", Nothing = "Select Method..", List = {"Above", "Below"}, Flag = "SW", 
	Callback = function(selected)
		Method = selected
	end
})

section1:AddSlider({
	Name = "Distance", Min = 7, Max = 100, Flag = "asdjjiwd",
	Callback = function(selected)
		Dist = selected
	end
})

section1:AddSlider({
	Name = "Npc Check Distance", Min = 1, Max = 500, Flag = "asdjjgiwd",
	Callback = function(selected)
		MAX_DISTANCE = selected
	end
})

section1:AddToggle({
	Name = "Auto BOSS", Value = false, Flag = "asdasdwggg", 
	Callback = function(state)
		l = state 
		farming = state
		while l do 
			if Bossname ~= nil then
				for i,v in pairs(workspace.Live:GetChildren()) do
					if v.Name:match(Bossname) then bosstarget = v end
				end
				if bosstarget and bosstarget:FindFirstChild("HumanoidRootPart") then 
					if Method == "Above" then
						HumanoidRootPart.CFrame = CFrame.new(bosstarget.HumanoidRootPart.Position + CFrame.new(0,Dist,0).Position, bosstarget.HumanoidRootPart.Position)
					else
						HumanoidRootPart.CFrame = CFrame.new(bosstarget.HumanoidRootPart.Position + CFrame.new(0,-Dist,0).Position, bosstarget.HumanoidRootPart.Position)
					end
				end 
			end
			task.wait()
		end
	end
})

section1:AddToggle({
	Name = "Auto Mobs (specific)", Value = false, Flag = "gggggg", 
	Callback = function(state)
		l = state 
		farming = state
		while l do 
			local target = getClosestMob()
			if target and target:FindFirstChild("HumanoidRootPart") and target.Name:match(Mobname) then 
				if Method == "Above" then
					HumanoidRootPart.CFrame = CFrame.new(target.HumanoidRootPart.Position + CFrame.new(0,Dist,0).Position, target.HumanoidRootPart.Position)
				else
					HumanoidRootPart.CFrame = CFrame.new(target.HumanoidRootPart.Position + CFrame.new(0,-Dist,0).Position, target.HumanoidRootPart.Position)
				end
			end 
			task.wait()
		end
		task.wait()
	end
})

section1:AddToggle({
	Name = "Auto Near Mobs", Value = false, Flag = "NB", 
	Callback = function(state)
		l = state 
		farming = state
		while l do 
			local target = getClosestMob()
			if target and target:FindFirstChild("HumanoidRootPart") then 
				if Method == "Above" then
					HumanoidRootPart.CFrame = CFrame.new(target.HumanoidRootPart.Position + CFrame.new(0,Dist,0).Position, target.HumanoidRootPart.Position)
				else
					HumanoidRootPart.CFrame = CFrame.new(target.HumanoidRootPart.Position + CFrame.new(0,-Dist,0).Position, target.HumanoidRootPart.Position)
				end
			end 
			task.wait()
		end
		task.wait()
	end
})


-- ══════════════════════════════════════════
--         SECTION 11 — AUTO ARROW
-- ══════════════════════════════════════════
local skibiditoilet = section11:AddLabel({ Name = "Stand Info" })
local Name = section11:AddLabel({ Name = "Name:" })
local Skin = section11:AddLabel({ Name = "Skin:" })
local Trait = section11:AddLabel({ Name = "n/a:" })
local Speed = section11:AddLabel({ Name = "n/a:" })
local Strength = section11:AddLabel({ Name = "n/a:" })
local Speciality = section11:AddLabel({ Name = "n/a:" })
section11:AddLabel({Name = "------------------------"})

----

local tbh = {[1] = "D",[2] = "C", [3] = "B", [4] = "A", [5] = "S"}
local StandTable = {}
local SkinTable = {}
-- {"Name":"Anubis","ID":"00098989-20ed-4bd5-81c7-dbd2a7e910df","Speed":3,"Strength":5,"Trait":"Determined","Specialty":5}
function getStand()

	local newstand = HttpService:JSONDecode(game:GetService("Players").LocalPlayer.PlayerData.SlotData.Stand.Value)

	Name:Set("Name: " .. tostring(newstand.Name))
	if newstand["Skin"] then
		Skin:Set("Skin: ".. tostring(newstand.Skin) )
	else
		newstand.Skin = nil
		Skin:Set("Skin: None" )

	end
	Trait:Set("Trait: " .. tostring(newstand.Trait))
	Speed:Set("Speed : " .. newstand.Speed .. "(" .. tostring(tbh[newstand.Speed])) ")"
	Speciality:Set("Speciality : " .. newstand.Specialty .. "(" .. tostring(tbh[newstand.Specialty]))  ")"
	Strength:Set("Strength : " .. newstand.Strength .. "(" .. tostring(tbh[newstand.Strength]))  ")"

	return newstand
end

getStand()
game:GetService("Players").LocalPlayer.PlayerData.SlotData.Stand.Changed:Connect(function()
	getStand()
end)

for i,v in pairs(game:GetService("ReplicatedStorage").assets.models.stands:GetChildren()) do
	if v.Name == "Skins" then

	else
		table.insert(StandTable,v.Name)
	end
end

for i,v in pairs(game:GetService("ReplicatedStorage").assets.models.stands.Skins:GetChildren()) do
	if v.Name == "Skins" then

	else
		table.insert(SkinTable,v.Name)
	end
end

section11:AddDropdown({
	Name = "Stand", Nothing = "Select Stand..", List = StandTable, Flag = "SelectStand", 
	Callback = function(selected)
		SelectStand = selected
	end
})

section11:AddDropdown({
	Name = "Skin", Nothing = "Select Skin..", List = SkinTable, Flag = "SelectSkin", 
	Callback = function(selected)
		SelectSkin = selected
	end
})

section11:AddToggle({
	Name = "Auto Roll Stand Arrow",
	Value = false,
	Flag = "AutoRollStandArrow",
	Callback = function(state)
		AutoRolling = state

		while AutoRolling do
			local stand = getStand()

			game:GetService("ReplicatedStorage")
				:WaitForChild("requests")
				:WaitForChild("character")
				:WaitForChild("use_item")
				:FireServer("Stand Arrow")

			if stand.Name == SelectStand or stand.Skin == SelectSkin then
				AutoRolling = false
				library.flags.AutoRollStandArrow = false -- force UI off
				break
			end

			task.wait()
		end
	end
})


-- ══════════════════════════════════════════
--         SECTION 5 — TPS
-- ══════════════════════════════════════════
section9:AddDropdown({
	Name = "Item",
	Nothing = "Select Item..",
	List = ItemList,
	Flag = "dfghdfg",
	Callback = function(selected)
		Item = selected
		print("Selected:", selected)
		print("Data:", ItemData[selected])
	end
})

local function checkItem(item)
	if item.Name ~= "purchase_template" then
		return
	end

	local title = item:FindFirstChild("title")
	local stock = item:FindFirstChild("stock")
	if not title or not stock then
		return
	end
	if title.Text == Item then
		local args = {
			Item
		}
		game:GetService("ReplicatedStorage"):WaitForChild("requests"):WaitForChild("character"):WaitForChild("cash_shop"):FireServer(unpack(args))
	end
end

section9:AddToggle({
	Name = "Start Shop Snipe",
	Value = false,
	Flag = "gfdlkhdfklghdkl",

	Callback = function(state)
		l = state
		
		while l do
			for _, item in ipairs(game.Players.LocalPlayer.PlayerGui:WaitForChild("Cash Shop"):WaitForChild("cash_shop"):WaitForChild("holder"):WaitForChild("goods_list"):WaitForChild("ScrollingFrame"):WaitForChild("holder"):GetChildren()) do
				checkItem(item)
			end
			task.wait(0.1)
		end
	end
})

section5:AddDropdown({
	Name = "NPC", Nothing = "Select NPC..", List = xx, Flag = "TP", 
	Callback = function(selected)
		Something = selected
	end
})

section5:AddButton({
	Name = "Teleport", 
	Callback = function()
		HumanoidRootPart.CFrame = game:GetService("ReplicatedStorage").assets["npc_cache"][tostring(Something)].HumanoidRootPart.CFrame
	end
})

section5:AddButton({
	Name = "Prompt Bus UI", 
	Callback = function()
		local args = {
			workspace:WaitForChild("Npcs"):WaitForChild("Rhett"),
			"Yes."
		}
		game:GetService("ReplicatedStorage"):WaitForChild("requests"):WaitForChild("character"):WaitForChild("dialogue"):FireServer(unpack(args))
	end
})


-- ══════════════════════════════════════════
--         SECTION 4 — RAIDS
-- ══════════════════════════════════════════
local function raidFightLoop()
	local target = getClosestMob()
	spawn(function()
		game:GetService("Players").LocalPlayer.Character:WaitForChild("client_character_controller"):WaitForChild("M1"):FireServer(true, false)
	end)
	spawn(function()
		if target and target:FindFirstChild("HumanoidRootPart") and not target.Name:match("Hostage") and not target.Name:match("Server") then 
			local offset = (Method == "Above") and CFrame.new(0,Dist,0).Position or CFrame.new(0,-Dist,0).Position
			HumanoidRootPart.CFrame = CFrame.new(target.HumanoidRootPart.Position + offset, target.HumanoidRootPart.Position)
		end 
	end)
	task.wait()
end

section4:AddToggle({
	Name = "Auto Kira Raid", Value = false, Flag = "AutoKira", 
	Callback = function(state)
		l = state 
		farming = state
		while l do 
			if tonumber(game.PlaceId) == 14890802310 then
				HumanoidRootPart.CFrame = CFrame.new(1036.37317, 875.602539, -650.364441)
				game:GetService("ReplicatedStorage").requests.character.dialogue:FireServer(
					workspace:WaitForChild("Npcs"):WaitForChild("Yoshikage Kira"), "Raid.")
				wait()
			elseif tonumber(game.PlaceId) == 74747090658891 then
				raidFightLoop()
			end
		end
		task.wait()
	end
})

section4:AddToggle({
	Name = "Auto DIO Raid", Value = false, Flag = "AutoDio", 
	Callback = function(state)
		l = state 
		farming = state
		while l do 
			if tonumber(game.PlaceId) == 14890802310 then
				HumanoidRootPart.CFrame = CFrame.new(2791.06689, 950.706116, 742.34613)
				game:GetService("ReplicatedStorage").requests.character.dialogue:FireServer(
					workspace:WaitForChild("Npcs"):WaitForChild("???"), "Raid.")
				wait()
			elseif tonumber(game.PlaceId) == 74747090658891 then
				raidFightLoop()
			end
		end
		task.wait()
	end
})

section4:AddToggle({
	Name = "Auto Jataro Raid", Value = false, Flag = "Jotaro", 
	Callback = function(state)
		l = state 
		farming = state
		while l do 
			if tonumber(game.PlaceId) == 14890802310 then
				HumanoidRootPart.CFrame = CFrame.new(1075.32422, 884.225525, 208.307587)
				game:GetService("ReplicatedStorage").requests.character.dialogue:FireServer(
					workspace:WaitForChild("Npcs"):WaitForChild("Chumbo"), "Raid.")
				wait()
			elseif tonumber(game.PlaceId) == 74747090658891 then
				raidFightLoop()
			end
		end
		task.wait()
	end
})

section4:AddToggle({
	Name = "Auto Avdol Raid", Value = false, Flag = "Avdol", 
	Callback = function(state)
		l = state 
		farming = state
		while l do 
			if tonumber(game.PlaceId) == 14890802310 then
				HumanoidRootPart.CFrame = CFrame.new(345.42511, 876.082092, 1017.30103)
				game:GetService("ReplicatedStorage").requests.character.dialogue:FireServer(
					workspace:WaitForChild("Npcs"):WaitForChild("Muhammad Avdol"), "Raid.")
				wait()
			elseif tonumber(game.PlaceId) == 74747090658891 then
				raidFightLoop()
			end
		end
		task.wait()
	end
})

section4:AddToggle({
	Name = "Auto Retry", Value = false, Flag = "eeqwrqweqwe", 
	Callback = function(state)
		l = state 
		while l do 
			if game.Players.LocalPlayer.PlayerGui:FindFirstChild("raidcomplete") then
				task.wait(Selectedtime2)
				game:GetService("ReplicatedStorage").requests.character.retryraid:FireServer()
			end
			task.wait()
		end 
		task.wait()
	end
})

section4:AddSlider({
	Name = "Auto Retry Delay", Decimals = 2, Min = 0.1, Max = 2, Flag = "AutoRetryTime",
	Callback = function(selected)
		Selectedtime2 = selected
	end
})

-- ══════════════════════════════════════════
--         SECTION 7 — AUTO SELL
-- ══════════════════════════════════════════
local function makeAutoSell(rarity, flag)
	section7:AddToggle({
		Name = "Auto Sell [" .. rarity .. "]", Value = false, Flag = flag,
		Callback = function(state)
			l = state 
			while l do 
				local CurrentInventory = game:GetService("HttpService"):JSONDecode(
					game:GetService("Players").LocalPlayer.PlayerData.SlotData.Inventory.Value)
				for _, Item in pairs(CurrentInventory) do
					if not l then break end
					if Data[Item.Name] and Data[Item.Name].Rarity == rarity then
						game:GetService("ReplicatedStorage").requests.general:WaitForChild("SellItem"):FireServer({
							{ ID = Item.ID, Pips = Item.Pips, Name = Item.Name, duplicates = 1 }
						})
					end
					task.wait(Selectedtime)
				end
			end
			task.wait()
		end
	})
end

makeAutoSell("Common",    "AutoSellCOmmon")
makeAutoSell("Uncommon",  "AutoSellUncommon")
makeAutoSell("Rare",      "AutoSellRAre")
makeAutoSell("Legendary", "AutoSellLegend")
makeAutoSell("Mythical",  "AutoSellMythic")

section7:AddSlider({
	Name = "Auto Sell Time", Decimals = 2, Min = 0.1, Max = 2, Flag = "AutoSellTime",
	Callback = function(selected)
		Selectedtime = selected
	end
})

-- ══════════════════════════════════════════
--         SECTION 8 — AUTO OPEN CHESTS
-- ══════════════════════════════════════════
local function makeAutoChest(rarity, flag)
	section8:AddToggle({
		Name = "Auto Open Chest [" .. rarity .. "]", Value = false, Flag = flag,
		Callback = function(state)
			l = state 
			while l do 
				game:GetService("ReplicatedStorage").requests.character.use_item:FireServer(
					rarity .. " Chest", { UseAll = true })
				task.wait()
			end 
			task.wait()
		end
	})
end

makeAutoChest("Common",    "AutoOpenChestCommon")
makeAutoChest("Rare",      "AutoOpenChestRare")
makeAutoChest("Legendary", "AutoOpenCHestLegendary")

-- ══════════════════════════════════════════
--         SECTION 6 — AUTO EXP
-- ══════════════════════════════════════════

section6:AddToggle({
	Name = "Auto HOLD E", Value = false, Flag = "AutoHoldE", 
	Callback = function(state)
		l = state 
		while l do HoldE() task.wait() end 
		task.wait()
	end
})

section6:AddToggle({
	Name = "Auto EXP (alt)", Value = false, Flag = "AutoEXPALT", 
	Callback = function(state)
		l = state 
		while l do 
			game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(1541.82776, 875.992432, -25.0460663)
			game.Players.LocalPlayer.Character.Humanoid.Health = 0
			task.wait()
		end 
		task.wait()
	end
})

section6:AddToggle({
	Name = "Auto EXP", Value = false, Flag = "AUTOEXPMAIN", 
	Callback = function(state)
		l = state 
		while l do 
			game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(1541.82776, 875.992432, -25.0460663)
			task.wait()
		end 
		task.wait()
	end
})

-- ══════════════════════════════════════════
--         SECTION 2 — AUTO'S
-- ══════════════════════════════════════════
section2:AddToggle({
	Name = "Only Enable When Farming", Value = false, Flag = "gghdfghdf", 
	Callback = function(state)
		disablewhenfarm = state 
	end
})
section2:AddToggle({
	Name = "Auto Summon Stand", Value = false, Flag = "Summon", 
	Callback = function(state)
		k = state 
		while k do 
			if not game.Players.LocalPlayer.Character:GetAttribute("SummonedStand") then
				game:GetService("Players").LocalPlayer.Character:WaitForChild("client_character_controller"):WaitForChild("SummonStand"):FireServer()
			end
			task.wait()
		end 
	end
})

local function makeAutoSkill(skillName, flag, key)
	section2:AddToggle({
		Name = "Auto Skill (" .. key .. ")", Value = false, Flag = flag, 
		Callback = function(state)
			k = state 
			while k do 
				if not k or (disablewhenfarm and not farming) then
					if not k then break end
				else
					game:GetService("Players").LocalPlayer.Character:WaitForChild("client_character_controller"):WaitForChild("Skill"):FireServer(key, true)
				end
				task.wait()
			end 
		end
	})
end

section2:AddToggle({
	Name = "Auto M1", Value = false, Flag = "m1", 
	Callback = function(state)
		k = state 
		while k do 
			if not k or (disablewhenfarm and not farming) then
				if not k then break end
			else
				game:GetService("Players").LocalPlayer.Character:WaitForChild("client_character_controller"):WaitForChild("M1"):FireServer(true, false)
			end
			task.wait()
		end 
	end
})

section2:AddToggle({
	Name = "Auto M2", Value = false, Flag = "m2", 
	Callback = function(state)
		k = state 
		while k do 
			if not k or (disablewhenfarm and not farming) then
				if not k then break end
			else
				game:GetService("Players").LocalPlayer.Character:WaitForChild("client_character_controller"):WaitForChild("M2"):FireServer(true, false)
			end
			task.wait()
		end 
	end
})

makeAutoSkill("E", "E", "E")
makeAutoSkill("R", "R", "R")
makeAutoSkill("Z", "Z", "Z")
makeAutoSkill("X", "X", "X")
makeAutoSkill("C", "C", "C")
makeAutoSkill("V", "V", "V")

-- ══════════════════════════════════════════
--         SECTION 10 — MISC / MOVEMENT
-- ══════════════════════════════════════════
local Player = game.Players.LocalPlayer
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local Camera = workspace.CurrentCamera

local ForcedSpeed = 12.5
local WalkSpeedToggled = false
local WalkSpeedConnection
local CurrentHumanoidSpeed

local ForcedJump = 50
local JumpHeightToggled = false
local JumpConnection
local CurrentHumanoidJump

local function ApplyWalkSpeed()
	local Char = Player.Character or Player.CharacterAdded:Wait()
	local Humanoid = Char:FindFirstChildOfClass("Humanoid")
	if not Humanoid then return end
	CurrentHumanoidSpeed = Humanoid
	Humanoid.WalkSpeed = ForcedSpeed
	if WalkSpeedConnection then WalkSpeedConnection:Disconnect() end
	WalkSpeedConnection = Humanoid:GetPropertyChangedSignal("WalkSpeed"):Connect(function()
		if Humanoid.WalkSpeed ~= ForcedSpeed then Humanoid.WalkSpeed = ForcedSpeed end
	end)
end

local function ResetWalkSpeed()
	if WalkSpeedConnection then WalkSpeedConnection:Disconnect() WalkSpeedConnection = nil end
	CurrentHumanoidSpeed = nil
end

local function ApplyJumpPower()
	local Char = Player.Character or Player.CharacterAdded:Wait()
	local Humanoid = Char:FindFirstChildOfClass("Humanoid")
	if not Humanoid then return end
	CurrentHumanoidJump = Humanoid
	Humanoid.JumpPower = ForcedJump
	if JumpConnection then JumpConnection:Disconnect() end
	JumpConnection = Humanoid:GetPropertyChangedSignal("JumpPower"):Connect(function()
		if Humanoid.JumpPower ~= ForcedJump then Humanoid.JumpPower = ForcedJump end
	end)
end

local function ResetJumpPower()
	if JumpConnection then JumpConnection:Disconnect() JumpConnection = nil end
	CurrentHumanoidJump = nil
end

-- Freecam
local fcRunning = false
ContextActionService = game:GetService("ContextActionService")

workspace:GetPropertyChangedSignal("CurrentCamera"):Connect(function()
	local newCamera = workspace.CurrentCamera
	if newCamera then Camera = newCamera end
end)

local INPUT_PRIORITY = Enum.ContextActionPriority.High.Value
local cameraFov

Spring = {} do
	Spring.__index = Spring
	function Spring.new(freq, pos)
		local self = setmetatable({}, Spring)
		self.f = freq; self.p = pos; self.v = pos*0
		return self
	end
	function Spring:Update(dt, goal)
		local f = self.f*2*math.pi
		local p0, v0 = self.p, self.v
		local offset = goal - p0
		local decay = math.exp(-f*dt)
		self.p = goal + (v0*dt - offset*(f*dt + 1))*decay
		self.v = (f*dt*(offset*f - v0) + v0)*decay
		return self.p
	end
	function Spring:Reset(pos) self.p = pos; self.v = pos*0 end
end

local cameraPos = Vector3.new()
local cameraRot = Vector2.new()
local velSpring = Spring.new(5, Vector3.new())
local panSpring = Spring.new(5, Vector2.new())

Input = {} do
	local keyboard = { W=0,A=0,S=0,D=0,E=0,Q=0,Up=0,Down=0,LeftShift=0 }
	local mouse = { Delta = Vector2.new() }
	local NAV_KEYBOARD_SPEED = Vector3.new(1,1,1)
	local PAN_MOUSE_SPEED = Vector2.new(1,1)*(math.pi/64)
	local NAV_ADJ_SPEED = 0.75
	local NAV_SHIFT_MUL = 0.25
	local navSpeed = 1

	function Input.Vel(dt)
		navSpeed = math.clamp(navSpeed + dt*(keyboard.Up - keyboard.Down)*NAV_ADJ_SPEED, 0.01, 4)
		local kKeyboard = Vector3.new(keyboard.D-keyboard.A, keyboard.E-keyboard.Q, keyboard.S-keyboard.W)*NAV_KEYBOARD_SPEED
		local shift = UserInputService:IsKeyDown(Enum.KeyCode.LeftShift)
		return kKeyboard*(navSpeed*(shift and NAV_SHIFT_MUL or 1))
	end

	function Input.Pan(dt)
		local kMouse = mouse.Delta*PAN_MOUSE_SPEED
		mouse.Delta = Vector2.new()
		return kMouse
	end

	local function Keypress(action, state, input)
		keyboard[input.KeyCode.Name] = state == Enum.UserInputState.Begin and 1 or 0
		return Enum.ContextActionResult.Sink
	end
	local function MousePan(action, state, input)
		local delta = input.Delta
		mouse.Delta = Vector2.new(-delta.y, -delta.x)
		return Enum.ContextActionResult.Sink
	end
	local function Zero(t) for k,v in pairs(t) do t[k] = v*0 end end

	function Input.StartCapture()
		ContextActionService:BindActionAtPriority("FreecamKeyboard",Keypress,false,INPUT_PRIORITY,
			Enum.KeyCode.W,Enum.KeyCode.A,Enum.KeyCode.S,Enum.KeyCode.D,
			Enum.KeyCode.E,Enum.KeyCode.Q,Enum.KeyCode.Up,Enum.KeyCode.Down)
		ContextActionService:BindActionAtPriority("FreecamMousePan",MousePan,false,INPUT_PRIORITY,Enum.UserInputType.MouseMovement)
	end
	function Input.StopCapture()
		navSpeed = 1; Zero(keyboard); Zero(mouse)
		ContextActionService:UnbindAction("FreecamKeyboard")
		ContextActionService:UnbindAction("FreecamMousePan")
	end
end

local function GetFocusDistance(cameraFrame)
	local znear = 0.1
	local viewport = Camera.ViewportSize
	local projy = 2*math.tan(cameraFov/2)
	local projx = viewport.x/viewport.y*projy
	local fx,fy,fz = cameraFrame.rightVector, cameraFrame.upVector, cameraFrame.lookVector
	local minDist = 512
	local minVect = Vector3.new()
	for x=0,1,0.5 do
		for y=0,1,0.5 do
			local cx = (x-0.5)*projx
			local cy = (y-0.5)*projy
			local offset = fx*cx - fy*cy + fz
			local origin = cameraFrame.p + offset*znear
			local _, hit = workspace:FindPartOnRay(Ray.new(origin, offset.unit*minDist))
			local dist = (hit-origin).magnitude
			if minDist > dist then minDist = dist; minVect = offset.unit end
		end
	end
	return fz:Dot(minVect)*minDist
end

local PlayerState = {} do
	local mouseBehavior, mouseIconEnabled, cameraType, cameraFocus, cameraCFrame, cameraFieldOfView
	function PlayerState.Push()
		cameraFieldOfView = Camera.FieldOfView
		Camera.FieldOfView = 70
		cameraType = Camera.CameraType
		Camera.CameraType = Enum.CameraType.Custom
		cameraCFrame = Camera.CFrame
		cameraFocus = Camera.Focus
		mouseIconEnabled = UserInputService.MouseIconEnabled
		UserInputService.MouseIconEnabled = true
		mouseBehavior = UserInputService.MouseBehavior
		UserInputService.MouseBehavior = Enum.MouseBehavior.Default
	end
	function PlayerState.Pop()
		Camera.FieldOfView = cameraFieldOfView
		Camera.CameraType = cameraType
		Camera.CFrame = cameraCFrame
		Camera.Focus = cameraFocus
		UserInputService.MouseIconEnabled = mouseIconEnabled
		UserInputService.MouseBehavior = mouseBehavior
	end
end

local function StepFreecam(dt)
	local vel = velSpring:Update(dt, Input.Vel(dt))
	local pan = panSpring:Update(dt, Input.Pan(dt))
	local zoomFactor = math.sqrt(math.tan(math.rad(70/2))/math.tan(math.rad(cameraFov/2)))
	cameraRot = cameraRot + pan*Vector2.new(0.75,1)*8*(dt/zoomFactor)
	cameraRot = Vector2.new(math.clamp(cameraRot.x, -math.rad(90), math.rad(90)), cameraRot.y%(2*math.pi))
	local cf = CFrame.new(cameraPos)*CFrame.fromOrientation(cameraRot.x, cameraRot.y, 0)*CFrame.new(vel*Vector3.new(1,1,1)*64*dt)
	cameraPos = cf.p
	Camera.CFrame = cf
	Camera.Focus = cf*CFrame.new(0,0,-GetFocusDistance(cf))
	Camera.FieldOfView = cameraFov
end

local function StartFreecam(pos)
	if fcRunning then StopFreecam() end
	local cf = pos or Camera.CFrame
	cameraRot = Vector2.new()
	cameraPos = cf.Position
	cameraFov = Camera.FieldOfView
	velSpring:Reset(Vector3.new())
	panSpring:Reset(Vector2.new())
	PlayerState.Push()
	RunService:BindToRenderStep("Freecam", Enum.RenderPriority.Camera.Value, StepFreecam)
	Input.StartCapture()
	fcRunning = true
end

function StopFreecam()
	if not fcRunning then return end
	Input.StopCapture()
	RunService:UnbindFromRenderStep("Freecam")
	PlayerState.Pop()
	Camera.FieldOfView = 70
	fcRunning = false
end

section10:AddToggle({
	Name = "Freecam", Value = false, Flag = "freecamToggle",
	Callback = function(State)
		if State then if not fcRunning then StartFreecam() end
		else if fcRunning then StopFreecam() end end
	end
})

Player.CharacterAdded:Connect(function()
	if fcRunning then StopFreecam() end
end)

local function missing(t, f, fallback)
	if type(f) == t then return f end
	return fallback
end
getgc = missing("function", getgc or get_gc_objects)

section10:AddToggle({
	Name = "Noclip Camera",
	Callback = function(_)
		local sc = (debug and debug.setconstant) or setconstant
		local gc = (debug and debug.getconstants) or getconstants
		local pop = game.Players.LocalPlayer.PlayerScripts.PlayerModule.CameraModule.ZoomController.Popper
		for _, v in pairs(getgc()) do
			if type(v) == "function" and getfenv(v).script == pop then
				for i, v1 in pairs(gc(v)) do
					if tonumber(v1) == 0.25 then sc(v, i, 0)
					elseif tonumber(v1) == 0 then sc(v, i, 0.25) end
				end
			end
		end
	end
})

local preMaxZoom = Player.CameraMaxZoomDistance
section10:AddToggle({
	Name = "Infinite Zoom", Value = false, Flag = "infiniteZoomToggle",
	Callback = function(State)
		l = State
		game.Players.LocalPlayer.CameraMaxZoomDistance = l and math.huge or preMaxZoom
	end
})

section10:AddSlider({
	Name = "Fov", Min = 30, Max = 120, Value = 70, Flag = "dfghdfghdf",
	Callback = function(Value)
		workspace.CurrentCamera.FieldOfView = tonumber(Value) or 70
	end
})

section10:AddToggle({
	Name = "Enable Walkspeed", Value = false, Flag = "walkspeedToggle",
	Callback = function(State)
		WalkSpeedToggled = State
		if State then ApplyWalkSpeed() else ResetWalkSpeed() end
	end
})

section10:AddSlider({
	Name = "Walkspeed", Min = 1, Max = 300, Value = 12.5, Flag = "walkspeedFlag",
	Callback = function(Value)
		ForcedSpeed = tonumber(Value) or 12.5
		if WalkSpeedToggled then ApplyWalkSpeed() end
	end
})

section10:AddToggle({
	Name = "Enable JumpHeight", Value = false, Flag = "jumpHeightToggle",
	Callback = function(State)
		JumpHeightToggled = State
		if State then ApplyJumpPower() else ResetJumpPower() end
	end
})

section10:AddSlider({
	Name = "JumpHeight", Min = 1, Max = 500, Value = 50, Flag = "jumpHeightFlag",
	Callback = function(Value)
		ForcedJump = tonumber(Value) or 50
		if JumpHeightToggled then ApplyJumpPower() end
	end
})


local configPersist = configSection:AddPersistence({
	Name = "Config",
	Value = DEFAULT_CONFIG,
	Workspace = CONFIG_FOLDER,
	Persistive = true,
	PostLoadCallback = function()
		print("[ZyrcHub] Config loaded!")
	end,
	PostSaveCallback = function()
		print("[ZyrcHub] Config saved!")
	end,
	Autoload = true
})

print('loaded')

-- ══════════════════════════════════════════
--         ANTI-AFK
-- ══════════════════════════════════════════
for i,v in pairs(getconnections(Player.Idled)) do 
	v:Disable()
end
